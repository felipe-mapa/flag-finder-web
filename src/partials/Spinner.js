import React from 'react';

const spinner = () => (
    <div>

        <div className="loader"
            >Loading...
        </div>
        <div className="loader__margin"></div>
    </div>
);

export default spinner;